export const metadata = {
    title: "AI Transformation | IntraWeb Technologies",
    description: "AI strategy, diagnostics, and operational redesign for executive and operator audiences",
};

export default function AITransformationLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return <>{children}</>;
}
